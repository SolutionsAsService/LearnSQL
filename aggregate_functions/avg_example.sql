-- This query demonstrates how to calculate the average of a column.
SELECT AVG(age) AS average_age
FROM users;

-- Explanation:
-- `AVG()` calculates the average of the values in the specified column (`age`).
