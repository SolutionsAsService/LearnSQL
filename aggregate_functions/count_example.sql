-- This query demonstrates how to count the number of users.
SELECT COUNT(*) AS total_users
FROM users;

-- Explanation:
-- `COUNT(*)` is an aggregate function that returns the total number of rows.
-- `AS` assigns a name (`total_users`) to the result.
