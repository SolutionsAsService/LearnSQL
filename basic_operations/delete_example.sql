-- This query demonstrates how to delete records from a table.
DELETE FROM users
WHERE age < 20;

-- Explanation:
-- `DELETE FROM` removes records from the table.
-- `WHERE` ensures only specific records are deleted.
